package lesson6;

public class Cat extends Animal{

    public Cat(String type, String name, int maxJumpDistance, int maxRunDistance, int maxSwimDistance) {
        super(type, name, maxJumpDistance, maxRunDistance, maxSwimDistance);
    }
}
