package lesson6;

public class Dog extends Animal{

    public Dog(String type, String name, int maxJumpDistance, int maxRunDistance, int maxSwimDistance) {
        super(type, name, maxJumpDistance, maxRunDistance, maxSwimDistance);
    }
}
